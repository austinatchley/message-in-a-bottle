import { Link } from "@remix-run/react";

export default function NoteIndexPage() {
  return (
    <div>
      No note selected. Select a note on the left. Create new notes from the
      bottle interface.
    </div>
  );
}
