import { Link } from "@remix-run/react";

export default function BoardIndexPage() {
  return (
    <p>
      To find a board, scan its QR code and save the link. All boards are private by convention
    </p>
  );
}
